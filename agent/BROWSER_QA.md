# 화면 검수 사전점검과 재개 계약

월~토의 모든 활성 편집 계약에 적용한다. 기존 계약의 `COMPLETE` 즉시 종료나
`READY` 스테이징 안내보다 이 문서의 검수·배포 상태 구분이 우선한다.

## 제작 전에 확인

저장소 상태와 기존 묶음을 확인한 뒤, **리서치·새 원고·이미지 생성 전에** 다음을
수행한다. 과거 성공 기록, 도구 목록, 정적 검사만으로 준비됐다고 판정하지 않는다.

1. 제공된 브라우저 도구의 현재 사용 지침을 읽는다. Codex 인앱 브라우저를
   우선하며 도구가 지원하는 미리보기 방식과 뷰포트/스크린샷 기능을 확인한다.
   `file://`를 기본 미리보기 주소로 만들거나 시험 삼아 열지 않는다.
2. 최종 원고와 동일한 미리보기 방식으로, 민감정보 없는 검수용 샘플을 실제로
   열고 데스크톱·390px 모바일 화면 캡처가 모두 가능한지 확인한다. 공개 웹사이트가
   열린다는 이유만으로 로컬 원고도 검수 가능하다고 판단하지 않는다.
   지원되는 원고 미리보기 경로가 없으면 `QA_ENV_BLOCKED`다.
3. 실제 도구·미리보기 방식·확인 시각·두 화면의 증거 경로를 실행 메모에 기록한다.
   산출물은 `/tmp/blog-writing-qa/` 아래에 보관한다. 이 단계 성공은
   `QA_ENV_READY`이며 **당일 글의 최종 QA 통과는 아니다**.

명시적 보안 거절이 아니라 단순 일시 오류일 때만 동일 동작을 한 번 재시도한다.
브라우저 보안 거절은 HTTP 서버, 다른 URL/브라우저, 프록시, 인코딩, 간접 실행으로
우회하지 않는다. 샌드박스 실행 권한 문제는 지원되는 정식 승인 절차만 사용한다.
서버 설치·권한 완화·추가 공개 배포를 검수 복구라는 이유로 임의 수행하지 않는다.

준비되지 않으면 검수 환경은 `QA_ENV_BLOCKED`로 기록하되, 독립적으로 허용된
공개 자료 조사·텍스트 집필까지 중단하지 않는다. 보안 거절 대상의 콘텐츠를 다른
경로로 가져오거나 서버를 실행하는 허가는 아니다. 원고는 `.local-drafts/YYYY-MM-DD.md`,
검증 근거·요일 역할·남은 일을 `.local-drafts/YYYY-MM-DD.status.md`에 저장하고
`DRAFT_QA_PENDING`으로 보고한다. 이 폴더는 Git 추적·Pages·발행 도우미 입력에서 제외한다.
환경이 막힌 동안 새 이미지 생성과 최종 게시 번들 조립은 보류하여 재제작 비용을 막는다.
초안은 정식 게시 원고나 `COMPLETE`가 아니며, 정적 검사는 화면 검수를 대체하지 않는다.
보고에는 실제 오류와 필요한 권한/지원 경로를 명시하며 콘텐츠 품질 실패나 GitHub
인증 오류로 오진하지 않는다. 별도 자동 재시도 예약을 만들지 않는다.

매 실행은 과거 채팅이 아니라 저장소 계약과 날짜별 파일을 기준으로 시작한다.
당일 `.local-drafts/` 원고가 있으면 내용을 확인해 이어 쓰고 처음부터 재생성하지 않는다.
초안만 저장된 상태는 Git 작업 트리 오염이나 정식 번들의 PARTIAL로 오인하지 않는다.
검수 환경이 허용되면 초안을 정식 JSON으로 옮겨 근거·독자 품질·이미지·HTML·가드·
실화면 QA를 모두 수행한 뒤에만 배포한다. 이전 날짜의 보완은 사용자 요청이 있을 때만 한다.

## 최종 검수와 중단 후 재개

### 제한된 로컬 미리보기 명령

지원 도구가 localhost를 허용하고 필요한 서버 실행 권한이 승인된 환경에서는
`sh scripts/blog-env python3 -m blog_pipeline.publishing.preview_server --draft-id YYYY-MM-DD`
를 사용한다. 준비 확인에는 이미 완성된 최근 원고 ID를 쓰고, 최종 확인에는 당일 ID를 쓴다.
여러 원고의 보완 요청은 `--draft-id`를 반복 지정할 수 있다.
인앱 브라우저 주소는 `http://127.0.0.1:8765/preview/YYYY-MM-DD.html`이다.
선택한 미리보기 HTML·WebP·CSS만 제공하며 저장소 디렉터리, 원고 JSON, `.git`,
환경 파일은 제공하지 않는다. 검수 종료 후 Ctrl-C로 서버를 종료한다.
서버는 127.0.0.1에만 바인딩하며 외부 공개 호스트로 바꾸지 않는다.
이 명령은 보안 거절을 해제하거나 실행 승인을 대신하지 않는다. 앞선 거절이 있다면
정식 승인이 확인되기 전 실행하지 않는다. 단순 페이지 이동 시간 초과는 실제 화면과
서버 응답을 확인하여 구분하고, 로딩된 페이지를 보안 차단으로 오진하지 않는다.

- 사전점검에서 확인한 동일한 방식으로 최종 HTML을 데스크톱·390px 모바일에서
  실제 검수한다. 이미지 누락, 가로 넘침, 한글/말풍선 가독성, 광고 전후 연결을
  확인한다. 파일이 바뀌면 영향을 받은 화면을 다시 확인한다.
- JSON 가드 `COMPLETE`, 발행 묶음 `READY`, 테스트 성공, 자동 독자 점수는
  실화면 QA를 대신하지 않는다. 실제로 보지 않은 화면에 통과를 기록하지 않는다.
- 최종 QA가 막히면 `QA_BLOCKED`로 기록하고 원고·이미지·기존 스테이징을 보존한다.
  그 콘텐츠는 커밋·푸시·Pages 공개하지 않는다. `LOCAL_COMPLETE`라고 보고하지 않는다.
- 재개 시 `publish_bundle --resume-check`와 이전 실행 메모를 함께 확인한다.
  콘텐츠 `COMPLETE`여도 QA 증거가 없으면 종료하거나 스테이징부터 진행하지 않고
  **누락된 QA부터** 재개한다. 유효한 원고·이미지는 재생성하지 않는다.
- 이미 명시적으로 차단된 대상은 이 계약을 근거로 우회하지 않는다. 허용 경로 또는
  권한 변경이 정식으로 확인되기 전까지 해당 QA는 미완료로 유지한다.
- QA와 로컬 검증이 모두 끝난 뒤에만 `REPOSITORY_SYNC.md`에 따라 배포한다.
  게시 도우미의 Pages 확인과 티스토리 실제 발행은 별도이며 티스토리는 사용자가 한다.

사전점검은 뒤늦은 실패와 불필요한 재제작을 줄인다. 실행 중 권한·도구 상태 변화까지
막거나 이후 무중단을 보장하지는 않는다.
